import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/app/app';

ReactDOM.render(<App/>, document.getElementById('root'));//вывести на страницу, render-отображать,(элемент(что), место(куда))

